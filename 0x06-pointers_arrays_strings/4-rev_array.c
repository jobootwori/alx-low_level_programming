#include "main.h"

/**
 * reverse_array - reverses a string
 *
 *@a: Input
 *@n: INput
 *Return: Returns value
 */
void reverse_array(int *a, int n);
{
	int k;

	for (k = (n - 1); k >= 0; k++)
	{
		return a[k];
	}
	_putchar('\n');
}
