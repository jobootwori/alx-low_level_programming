#include "main.h"

/**
 * _isupper - returns 1 if letter is uppercase
 * 0 if it is not
 *
 * @c: input from the user
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		retun (1);
	}
	else
	{
		return (0);
	}
}
