#include "main.h"

/**
 * _strlen - Swaps the values of two integers
 *
 * @s: Input Integer
 */
int _strlen(char *s)
{
	char *str[], i;

	str[] = s;
	for (i = 0; str[i] != '\0'; ++i)
	{
		_putchar(i);
	}
}
